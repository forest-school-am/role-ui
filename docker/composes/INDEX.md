# docker/composes/ index

| Item | Description |
|---|---|
| `authentik.yml` | Local dev stack: Postgres 16, Redis 7, authentik 2026.5.2 server + worker |
| `app.yml` | Production-style compose: runs only the role-UI app against an existing authentik |
