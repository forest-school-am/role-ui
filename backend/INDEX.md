# backend/ index

| Item | Description |
|---|---|
| `src/` | All Rust source files |
| `macros/` | Proc-macro helper crate (`autentik_macros`) used by extractors |
| `Cargo.toml` | Crate manifest — dependencies and version |
| `Cargo.lock` | Locked dependency tree |
