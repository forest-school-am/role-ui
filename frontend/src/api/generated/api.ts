/**
 * Generated by orval v8.16.0 🍺
 * Do not edit manually.
 * Authentik Role UI Backend
 * OpenAPI spec version: 1.0.0
 */
import { customMutator } from '../mutator';
export type GroupRole = typeof GroupRole[keyof typeof GroupRole];


export const GroupRole = {
  leader: 'leader',
  manager: 'manager',
  member: 'member',
} as const;

export interface UserLink {
  username: string;
  name: string;
}

export interface GroupLink {
  name: string;
}

export interface LoginAccount {
  /** e.g. "email", "telegram", "google" */
  kind: string;
  address: string;
}

/**
 * Members of a group split by their role.
 */
export interface GroupRoleSplit {
  leader: UserLink[];
  manager: UserLink[];
  member: UserLink[];
}

/**
 * Groups a user belongs to, split by their role in each group.
 */
export interface UserGroupRoleSplit {
  leader: GroupLink[];
  manager: GroupLink[];
  member: GroupLink[];
}

export interface User {
  username: string;
  name: string;
  is_active: boolean;
  logins: LoginAccount[];
  groups: UserGroupRoleSplit;
  /**
     * @items.minItems 2
     * @items.maxItems 2
     */
  attributes: [string, string][];
}

export interface Group {
  name: string;
  members: GroupRoleSplit;
  children: GroupLink[];
  parents: GroupLink[];
  color?: string;
}

export type _SearchResultUserSearchType = typeof _SearchResultUserSearchType[keyof typeof _SearchResultUserSearchType];


export const _SearchResultUserSearchType = {
  user: 'user',
} as const;

export interface SearchResultUser {
  __search_type: _SearchResultUserSearchType;
  username: string;
  name: string;
}

export type _SearchResultGroupSearchType = typeof _SearchResultGroupSearchType[keyof typeof _SearchResultGroupSearchType];


export const _SearchResultGroupSearchType = {
  group: 'group',
} as const;

export interface SearchResultGroup {
  __search_type: _SearchResultGroupSearchType;
  name: string;
}

export type SearchResult = SearchResultUser | SearchResultGroup;

export interface ApiError {
  error: string;
}

/**
 * Missing or invalid bearer token.
 */
export type UnauthorizedResponse = ApiError;

/**
 * Caller lacks the required role in this group.
 */
export type ForbiddenResponse = ApiError;

/**
 * User or group not found.
 */
export type NotFoundResponse = ApiError;

/**
 * Invalid request parameters or body.
 */
export type BadRequestResponse = ApiError;

export type SearchUsersParams = {
/**
 * Regex pattern to match against username, name, and login addresses.
 */
search?: string;
};

export type DisbandGroup200 = { [key: string]: unknown };

export type AddMemberBody = {
  username: string;
};

export type AddMember200 = { [key: string]: unknown };

export type RemoveMember200 = { [key: string]: unknown };

export type AddManagerBody = {
  username: string;
};

export type AddManager200 = { [key: string]: unknown };

export type RemoveManager200 = { [key: string]: unknown };

export type ResignLeaderBody = {
  /** Username of the manager who will become the new leader. */
  username: string;
};

export type ResignLeader200 = { [key: string]: unknown };

export type CreateSubgroupBody = {
  /**
     * @minLength 1
     * @maxLength 150
     */
  name: string;
};

export type AddChildGroupBody = {
  /** Name of the existing group to attach as a child. */
  group_name: string;
};

export type AddChildGroup200 = { [key: string]: unknown };

export type DetachChildGroup200 = { [key: string]: unknown };

export type SetGroupColorBody = {
  color: string;
};

export type SetGroupColor200 = { [key: string]: unknown };

export type SearchAllParams = {
/**
 * Search term. Treated as a regex for users; substring match for groups.
 */
q: string;
/**
 * Comma-separated list of result types to include. Defaults to `"user,group"`.
 */
types?: string;
};

export const getAuthentikRoleUIBackend = () => {
/**
 * Returns users whose username, display name, or login addresses match the `search` regex. Returns an empty array when `search` is absent or blank.
 * @summary Search users
 */
const searchUsers = (
    params?: SearchUsersParams,
 ) => {
      return customMutator<UserLink[]>(
      {url: `/users`, method: 'GET',
        params
    },
      );
    }

/**
 * @summary Get caller's full profile
 */
const getMe = (

 ) => {
      return customMutator<User>(
      {url: `/users/me`, method: 'GET'
    },
      );
    }

/**
 * @summary Get a user by username
 */
const getUser = (
    username: string,
 ) => {
      return customMutator<User>(
      {url: `/users/${username}`, method: 'GET'
    },
      );
    }

/**
 * @summary List all groups
 */
const getGroups = (

 ) => {
      return customMutator<Group[]>(
      {url: `/groups`, method: 'GET'
    },
      );
    }

/**
 * @summary Get a group by name
 */
const getGroup = (
    groupName: string,
 ) => {
      return customMutator<Group>(
      {url: `/groups/${groupName}`, method: 'GET'
    },
      );
    }

/**
 * Permanently deletes the group. Requires caller to be the group's **leader**. Fails with 400 if the group has any children — detach or disband them first.
 * @summary Disband a group
 */
const disbandGroup = (
    groupName: string,
 ) => {
      return customMutator<DisbandGroup200>(
      {url: `/groups/${groupName}`, method: 'DELETE'
    },
      );
    }

/**
 * Adds a user to the group as a plain member. Idempotent: if the user is already a member in any role the call succeeds without changes. Requires caller to be **manager or leader**.
 * @summary Add a member
 */
const addMember = (
    groupName: string,
    addMemberBody: AddMemberBody,
 ) => {
      return customMutator<AddMember200>(
      {url: `/groups/${groupName}/members`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: addMemberBody
    },
      );
    }

/**
 * Removes a user from the group. Idempotent if the user is not a member. Constraints enforced by caller role:
 * - Leaders cannot be removed by anyone. - Managers can only be removed by a **leader**. - Plain members can be removed by any **manager or leader**.
 * @summary Remove a member
 */
const removeMember = (
    groupName: string,
    username: string,
 ) => {
      return customMutator<RemoveMember200>(
      {url: `/groups/${groupName}/members/${username}`, method: 'DELETE'
    },
      );
    }

/**
 * Grants the manager role to an existing group member. Requires caller to be the group's **leader**. The user must already be a member; idempotent if they are already a manager.
 * @summary Promote a member to manager
 */
const addManager = (
    groupName: string,
    addManagerBody: AddManagerBody,
 ) => {
      return customMutator<AddManager200>(
      {url: `/groups/${groupName}/managers`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: addManagerBody
    },
      );
    }

/**
 * Strips the manager role from a user, reverting them to a plain member. Requires caller to be the group's **leader**. Idempotent if the user is not a manager.
 * @summary Demote a manager
 */
const removeManager = (
    groupName: string,
    username: string,
 ) => {
      return customMutator<RemoveManager200>(
      {url: `/groups/${groupName}/managers/${username}`, method: 'DELETE'
    },
      );
    }

/**
 * Transfers leadership to a successor. The successor must be a current **manager** of the group; they are promoted to leader and removed from the managers list. Requires caller to be the current **leader**.
 * @summary Resign as leader
 */
const resignLeader = (
    groupName: string,
    resignLeaderBody: ResignLeaderBody,
 ) => {
      return customMutator<ResignLeader200>(
      {url: `/groups/${groupName}/leader/resign`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: resignLeaderBody
    },
      );
    }

/**
 * Creates a brand-new group as a child of this one. The calling **leader** is automatically added as a member and assigned as leader of the new group. Name must be 1–150 characters.
 * @summary Create a new child group
 */
const createSubgroup = (
    groupName: string,
    createSubgroupBody: CreateSubgroupBody,
 ) => {
      return customMutator<GroupLink>(
      {url: `/groups/${groupName}/subgroups`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: createSubgroupBody
    },
      );
    }

/**
 * Makes an existing group a child of this one. Requires caller to be the **leader** of the parent group. Rejected with 400 if the attachment would create a cycle in the group hierarchy.
 * @summary Attach an existing group as a child
 */
const addChildGroup = (
    groupName: string,
    addChildGroupBody: AddChildGroupBody,
 ) => {
      return customMutator<AddChildGroup200>(
      {url: `/groups/${groupName}/children`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: addChildGroupBody
    },
      );
    }

/**
 * Removes the parent–child relationship without deleting either group. Requires caller to be the **leader** of the parent group.
 * @summary Detach a child group
 */
const detachChildGroup = (
    groupName: string,
    childGroupName: string,
 ) => {
      return customMutator<DetachChildGroup200>(
      {url: `/groups/${groupName}/children/${childGroupName}`, method: 'DELETE'
    },
      );
    }

/**
 * Updates the display color of a group. Requires caller to be **manager or leader**. Color value is a free-form string (e.g. a hex code like `"FF0000"`).
 * @summary Set group color
 */
const setGroupColor = (
    groupName: string,
    setGroupColorBody: SetGroupColorBody,
 ) => {
      return customMutator<SetGroupColor200>(
      {url: `/groups/${groupName}/color`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: setGroupColorBody
    },
      );
    }

/**
 * Searches users and/or groups matching the query term. User matching uses the term as a regex against username, name, and login addresses. Group matching is a case-insensitive substring match on the group name. At least one result type must be requested.
 * @summary Mixed user + group search
 */
const searchAll = (
    params: SearchAllParams,
 ) => {
      return customMutator<SearchResult[]>(
      {url: `/search`, method: 'GET',
        params
    },
      );
    }

/**
 * Returns a static JavaScript function `generateSearchLink(obj)` that maps a search result object to its frontend URL. This endpoint is intentionally **unauthenticated** as it contains no user data.
 * @summary Frontend link-generator helper
 */
const getSearchLinkGen = (

 ) => {
      return customMutator<string>(
      {url: `/search-link-gen`, method: 'GET'
    },
      );
    }

return {searchUsers,getMe,getUser,getGroups,getGroup,disbandGroup,addMember,removeMember,addManager,removeManager,resignLeader,createSubgroup,addChildGroup,detachChildGroup,setGroupColor,searchAll,getSearchLinkGen}};
export type SearchUsersResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['searchUsers']>>>
export type GetMeResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['getMe']>>>
export type GetUserResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['getUser']>>>
export type GetGroupsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['getGroups']>>>
export type GetGroupResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['getGroup']>>>
export type DisbandGroupResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['disbandGroup']>>>
export type AddMemberResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['addMember']>>>
export type RemoveMemberResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['removeMember']>>>
export type AddManagerResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['addManager']>>>
export type RemoveManagerResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['removeManager']>>>
export type ResignLeaderResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['resignLeader']>>>
export type CreateSubgroupResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['createSubgroup']>>>
export type AddChildGroupResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['addChildGroup']>>>
export type DetachChildGroupResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['detachChildGroup']>>>
export type SetGroupColorResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['setGroupColor']>>>
export type SearchAllResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['searchAll']>>>
export type GetSearchLinkGenResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getAuthentikRoleUIBackend>['getSearchLinkGen']>>>
