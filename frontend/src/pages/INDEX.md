# frontend/src/pages/ index

| Item | Description |
|---|---|
| `StructurePage.tsx` | `/structure` — DAG canvas + preview panels |
| `GroupPage.tsx` | `/groups/:name` — full group detail page |
| `PersonalPage.tsx` | `/me` and `/users/:username` — user detail page |
