// Shared UI class constants — keep all site-wide style tokens here.
// Note: spacing classes (mb-*, gap-*) vary per call site and are NOT included.

/** Section header label: small-caps, gray, uppercase. */
export const SECTION_LABEL_CLS =
  'text-xs font-semibold text-gray-400 uppercase tracking-wide';

/** Tiny inline link button, indigo colour. */
export const MINI_LINK_BTN_CLS = 'text-xs text-indigo-600 hover:text-indigo-800';

/** Standard row in a member/item list. */
export const MEMBER_ROW_CLS =
  'flex items-center gap-2 py-1.5 px-2 hover:bg-gray-50 rounded group';
